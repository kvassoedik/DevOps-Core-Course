version: "3.9"

services:
  app:
    build: .
    container_name: devops-info-service
    ports:
      - "5000:5000"
    environment:
      HOST: 0.0.0.0
      PORT: 5000
      LOG_LEVEL: INFO
      VISITS_FILE: /data/visits
    volumes:
      - ./data:/data